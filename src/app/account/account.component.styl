.container {
    position: fixed;
    width: 100%;
    height: 100%;
    overflow: auto;
}

md-card {
    max-width: 600px;
    width: 100%;
}

md-input-container {
    width: 100%;
}

.account-menu {
    position: absolute;
    top: 20px;
    right: 20px;
}
